export const metadata = {
  title: 'LaunchPad — Join the Waitlist',
  description: 'Get early access to LaunchPad',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  );
}
